spring:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/bumblebeeDB
        username: root
        password: root
    servlet:
        multipart:
            max-request-size: 5MB
            max-file-size: 5MB
